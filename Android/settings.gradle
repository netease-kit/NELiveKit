pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven {
            setUrl("https://maven.faceunity.com/repository/maven-public/")
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            setUrl("https://maven.faceunity.com/repository/maven-public/")
        }
    }
}
rootProject.name = "Android"

include(":app")
include(":beauty-faceunity")
include(":entertainment-common")
include(":livestreamkit")
include(":livestreamkit-ui")
